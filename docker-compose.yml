
version: '3.7'

services:
  keyt-dev:
    container_name: keyt-dev
    build: .
    extra_hosts:
      k8s.local: 10.13.3.1
    environment:
      DRY_RUN: true
    volumes:
      - type: bind
        source: ~/
        target: /bind
